package ATMtasks;

//  create a interface of the denominations of notes availabe;
public interface Denominations {
	int getValue();
	int getCount();
	void setValue(int value);
	void setCount(int count);
}
